char print_selection;
skipspaces(char **p1) {
  (*p1)++;
  char *s_index;
  airac_observe(s_index, 0);
  skipspaces(&s_index);
}

print_prompt_response_ptr() {
  print_selection = strdup(print_prompt_response_ptr);
  skipspaces(print_selection);
}
