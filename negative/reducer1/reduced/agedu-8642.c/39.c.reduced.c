int smalloc_p, fds, run_httpd_i, run_httpd_tmp___19;
smalloc(p1) {
  smalloc_p = malloc(p1);
  return smalloc_p;
}

run_httpd_tmp() {
  char readbuf[1];
  smalloc(run_httpd_i);
  run_httpd_tmp___19 = -run_httpd_i;
  run_httpd_tmp___19 = 3;
  run_httpd_i = run_httpd_tmp___19;
  while (1) {
    fds = smalloc(sizeof(int));
    int ctx = fds;
    char data = readbuf;
    airac_observe(ctx, 0);
  }
}
