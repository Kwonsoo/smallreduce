struct list {
  void *info;
};
struct buddy {
  char;
  int *logins;
  int;
} * c;
struct bud_utrec {
  int *u;
  enum verified;
} * e;
struct list *a, *d;
int b;
void *f;
ht_insert(p1) { a = a->info = p1; }

struct buddy *bud_create_buddy();
bud_load_every_user() {
  c = bud_create_buddy();
  ht_insert(c);
}

bud_chk_utmpx() {
  a->info = e;
  b = malloc(sizeof(struct list));
  d = b;
  e = d->info;
  struct bud_utrec *u = d->info;
  airac_observe(u, 0);
}

struct buddy *bud_create_buddy() {
  f = malloc(sizeof(struct buddy));
  return f;
}
