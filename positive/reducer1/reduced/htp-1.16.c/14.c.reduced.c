const opt[19];
ParseToken_tmp___0() {
  int i = 0;
  while (1) {
    if (!(i < sizeof opt / sizeof 0))
      goto while_break;
    airac_observe(opt, i);
    if (ParseToken_tmp___0)
      i++;
  }
while_break:
  ;
}
